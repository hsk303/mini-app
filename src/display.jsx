let display=(props)=>{
    return(
        <div>
            <p>Name-{props.name}</p>
            <p>Email-{props.email}</p>
            <p>Phone-{props.number}</p>
        </div>
    );
}
export default display;